# ============================================================
# REP-LIE — AutoDL 服务器专用依赖清单
# 目标平台：AutoDL vGPU-32GB / CUDA 12.x / Python 3.10
# 说明：此文件合并了 requirements_lock.txt（原始环境）
#      和 requirements.txt（目标版本），解决了以下冲突：
#        - torch 2.0.1 → 2.2.2（CUDA 12.1 兼容）
#        - transformers 4.33.1 → 4.40.2（LLaMA-3/Qwen2/GQA支持）
#        - accelerate 0.21.0 → 0.29.3（get_balanced_memory API）
#        - tokenizers 0.13.3 → 0.19.1（与 transformers 4.40 匹配）
#        - triton 2.0.0 → 移除（torch 2.2 自带）
#        - nvidia-*-cu11 → 全部移除（CUDA 12 由驱动提供）
# ============================================================
#
# ⚠️  注意：torch 需要单独安装（需指定官方 CUDA wheel 源）
#   pip install torch==2.2.2 torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cu121
#
# ============================================================

# ------------------------------------------------------------
# HuggingFace 核心（LLM 必须版本）
# ------------------------------------------------------------
transformers==4.40.2       # LLaMA-3/Qwen2/GQA/MistralForCausalLM 支持
peft==0.13.2               # 保留 lock 版本（0.13 > 0.10，向后兼容，API 更稳定）
datasets==2.19.1           # ❌ 不使用 3.5.0：datasets 3.x 新版 HTTP API 与 hf-mirror.com 不兼容（镜像站元数据不完整），降级到 2.19.1
accelerate==0.29.3         # get_balanced_memory / infer_auto_device_map 需要 0.28+
huggingface-hub==0.26.0    # 保留 lock 版本
evaluate==0.4.3            # 保留 lock 版本
tokenizers==0.19.1         # 必须与 transformers 4.40 匹配（不能用 0.13.3）
safetensors==0.4.5         # 保留 lock 版本

# ------------------------------------------------------------
# 量化
# ------------------------------------------------------------
bitsandbytes==0.45.0       # 保留 lock 版本（比目标 0.43.1 更新，CUDA 12 支持更好）
loralib==0.1.2

# ------------------------------------------------------------
# FLOPs 统计
# ------------------------------------------------------------
fvcore==0.1.5.post20221221
thop==0.1.1.post2209072238

# ------------------------------------------------------------
# NNI（模型压缩框架，魔改版）
# ------------------------------------------------------------
# ⚠️ 代码中 TaylorPruner_change 等不在官方 NNI，需本地魔改版
# 如仅运行 main_llm.py，可跳过此依赖
nni==3.0rc1                # 与 lock 版本一致

# ------------------------------------------------------------
# 超参搜索
# ------------------------------------------------------------
optuna==4.1.0              # 保留 lock 版本
ray==2.40.0                # 保留 lock 版本

# ------------------------------------------------------------
# 科学计算
# ------------------------------------------------------------
numpy==1.24.3              # 保留 lock 版本（与其他包最兼容）
scipy==1.14.1
scikit-learn==1.5.2
pandas==1.5.3

# ------------------------------------------------------------
# 图像处理
# ------------------------------------------------------------
opencv-python==4.10.0.84   # 保留 lock 版本
pillow==11.0.0

# ------------------------------------------------------------
# 可视化
# ------------------------------------------------------------
contourpy==1.2.1           # matplotlib 依赖；1.3.x 要求 Python>=3.11，Python 3.10 必须锁 1.2.x
matplotlib==3.7.5          # 3.9.x 的依赖链会拉 contourpy>=1.3，降到 3.7.x 可安全兼容 Python 3.10
tensorboard==2.13.0
tensorboardX==2.6.2.2

# ------------------------------------------------------------
# Tokenizer / Protobuf 底层
# ------------------------------------------------------------
sentencepiece==0.2.0
protobuf==4.25.5           # 保留 lock 版本

# ------------------------------------------------------------
# 工具库（直接从 lock 保留）
# ------------------------------------------------------------
tqdm==4.66.5
packaging==24.1
filelock==3.11.0
regex==2024.9.11
requests==2.32.3
PyYAML==6.0.2
typing_extensions==4.12.2
sympy==1.13.3
networkx==3.4.1
psutil==6.1.0
click==8.1.7
colorama==0.4.6
tabulate==0.9.0
six==1.16.0
joblib==1.4.2
mpmath==1.3.0
Jinja2==3.1.4

# ------------------------------------------------------------
# NNI 依赖（从 lock 保留）
# ------------------------------------------------------------
colorlog==6.9.0
prettytable==3.11.0
schema==0.7.7
portalocker==3.1.1
pyarrow==17.0.0
xxhash==3.5.0
dill==0.3.6
multiprocess==0.70.14
fsspec==2023.6.0
alembic==1.14.0
SQLAlchemy==2.0.36
Mako==1.3.7
json-tricks==3.17.3
cloudpickle==3.1.0
astor==0.8.1
yacs==0.1.8

# ------------------------------------------------------------
# ❌ 以下包从 requirements_lock.txt 中排除（冲突/不需要）
# ------------------------------------------------------------
# torch==2.0.1             → 用 torch==2.2.2 (cu121) 替代（单独安装）
# torchvision==0.15.2      → 用 torchvision==0.17.2 替代（单独安装）
# torchaudio==2.0.2        → 不需要
# triton==2.0.0            → 已内嵌于 torch 2.2.2，单独安装会冲突
# nvidia-*-cu11 全套       → CUDA 12.x 环境不需要 cu11 包（驱动已提供）
# keras==2.13.1            → TensorFlow 依赖，LLM剪枝不需要
# tensorflow==2.13.1       → 不需要
# tensorflow-estimator     → 不需要
# tensorflow-io-gcs-filesystem → 不需要
# selenium==4.29.0         → 不需要
# undetected-chromedriver  → 不需要
# google-auth / google-auth-oauthlib → 不需要
# PythonWebHDFS            → 不需要
