# Core dependencies for REP-LIE LLM Pruning
# Python >= 3.10

# Deep Learning Framework
torch>=2.0.0
torchvision>=0.15.0

# Transformers & PEFT
transformers>=4.36.0
peft>=0.7.0

# Model Pruning (NNI)
nni>=3.0
# 如果 NNI 安装有问题，可尝试:
# pip install nni==2.8 -i https://pypi.tuna.tsinghua.edu.cn/simple

# FLOPs Counting
fvcore>=0.1.20230117

# Quantization (for 4bit loading)
bitsandbytes>=0.41.0

# Datasets
datasets>=2.14.0
dataloader>=0.14.0

# Zero-shot Evaluation
lm-eval>=0.4.0
# 或者安装完整版:
# pip install git+https://github.com/EleutherAI/lm-evaluation-harness.git

# ModelScope (for downloading models)
modelscope>=1.11.0

# HuggingFace Hub (for downloading models)
huggingface_hub>=0.19.0

# Utilities
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
tqdm>=4.65.0
pandas>=2.0.0

# Logging & Config
tensorboard>=2.14.0
pyyaml>=6.0
